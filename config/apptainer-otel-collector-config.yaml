# https://opentelemetry.io/docs/collector/configuration/

receivers:
  otlp: # [^1]
    protocols:
      grpc:
        endpoint: localhost:4317

processors:
  batch:

exporters:
  otlphttp/prometheus: # [^2]
    endpoint: "http://localhost:9090/api/v1/otlp" # [^3]
  # Tempo is opt-in: swap nop for this exporter in the traces pipeline below.
  # otlp/tempo:
  #   endpoint: localhost:4319
  #   tls:
  #     insecure: true
  nop:
  debug:
    verbosity: detailed

extensions:
  health_check:
    endpoint: localhost:13133
  zpages:
    endpoint: localhost:55679

service:
  extensions: [health_check, zpages]
  telemetry:
    metrics:
      level: detailed
      readers:
        - pull:
            exporter:
              prometheus:
                host: localhost
                port: 8888
                without_type_suffix: true
                without_units: true
  pipelines:
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlphttp/prometheus]
    # Spans are dropped, but the pipeline must exist: without it the OTLP
    # receiver serves no TraceService and every boom app logs an export error.
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [nop]

# [^1]: https://github.com/open-telemetry/opentelemetry-collector/tree/main/receiver/otlpreceiver

# [^2]:
#   * https://prometheus.io/docs/guides/opentelemetry/#send-opentelemetry-metrics-to-the-prometheus-server
#   * https://github.com/open-telemetry/opentelemetry-collector/blob/main/exporter/otlphttpexporter
#
#   The Prometheus OTLP receiver requires HTTP and protobuf, so use the otlphttp
#   exporter (which encodes as proto by default).

# [^3]: https://prometheus.io/docs/guides/opentelemetry/#enable-the-otlp-receiver
